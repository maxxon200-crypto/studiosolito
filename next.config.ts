import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* No bundled imagery yet — the site ships labeled placeholders. When real
     photos are added, configure `images` (formats: AVIF/WebP) here. */
};

export default nextConfig;
